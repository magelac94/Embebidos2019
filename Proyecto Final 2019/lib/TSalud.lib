/*** BeginHeader */
#define RIT_MAX		5000
#define	RIT_MIN		2000
/*** EndHeader */

/*** BeginHeader tarea_salud */
void tarea_salud(void* pdata);
/*** EndHeader */
void tarea_salud(void* pdata){
	int ritmo_cardiaco;

	while(1){
		ritmo_cardiaco = IO_getAnalogInput(*pdata);
		if(ritmo_cardiaco < RIT_MIN || ritmo_cardiaco > RIT_MAX)
		{
			//enviar_msj("El votija esta palmando");
			return 1;
		}
	}
}